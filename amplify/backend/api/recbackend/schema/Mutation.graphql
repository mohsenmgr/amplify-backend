type Mutation {
  # Add Example Row
  makeTodo(
    id: ID!
    userId: String!
    title: String!
    description: String
    photo: String
    createdAt: AWSDateTime
    updatedAt: AWSDateTime
    dueDate: AWSDateTime
    done: Boolean
  ): Todo @function(name: "todoLambda-${env}")
}
